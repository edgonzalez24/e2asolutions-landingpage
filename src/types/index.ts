export interface MenuItem {
  name: string;
  slug: string;
}

export interface StepItem {
  position: string;
  icon: string;
  title: string;
  description: string;
}

export interface FormItem {
  name: string;
  email: string;
  phone: string;
  message: string;
}